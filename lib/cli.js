'use strict'

const {argv:[,,...args]} = process,
      args1 = require('./parse-args.js')(args);
      // addToBag = require('./addToBag.js');
      console.log(args)
console.log(args1)
// console.log(addToBag)
